language: php
before_install:
  - nvm install node
install:
  - yarn install
  - rm -rf composer.lock
  - composer install
php:
  - '5.6'
  - '7.1'
before_script:
  - composer phpcbf
  - composer lint
scripts:
  - composer test
